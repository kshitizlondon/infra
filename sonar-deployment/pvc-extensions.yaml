apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: extensions
  namespace: sonar
  labels:
    app: sonarqube
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 500Mi
