apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: conf
  namespace: sonar
  labels:
    app: sonarqube
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 500Mi
